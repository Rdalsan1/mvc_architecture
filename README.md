Follow the steps below to run the application locally.

📦 Installation

Clone the repository:

git clone <your-repo-url>
cd <your-project-folder>


Install required dependencies:

npm install bcryptjs connect-flash connect-mongo dotenv ejs express express-rate-limit express-session express-validator fs method-override mongoose morgan multer path

▶️ Run the Application

npm start

The server will start on the configured port (default: http://localhost:3000/).
